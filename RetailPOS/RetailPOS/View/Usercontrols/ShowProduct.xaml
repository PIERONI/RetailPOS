<UserControl x:Class="RetailPOS.View.Usercontrols.ShowProduct"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
              d:DesignHeight="200" d:DesignWidth="490" DataContext="{Binding Source={StaticResource Locator}, Path=CategoryVM}">
   
    <UserControl.Resources>
      
    </UserControl.Resources>
    <Grid Width="490" Height="200">
        <ListBox Name="LstProduct" ItemsSource="{Binding lstProduct}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,0">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel  Orientation="Horizontal"  Margin="0"   Height="Auto" Width="490" >
                    </WrapPanel>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button Margin="4" Content="{Binding Name}" Height="53" Background="{Binding color}" HorizontalAlignment="Right" Width="79" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
